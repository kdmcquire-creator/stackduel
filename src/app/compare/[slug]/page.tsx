import products from '@/lib/products.json';\nimport { Product } from '@/lib/types';\nimport Image from 'next/image';\nimport Link from 'next/link';\nimport { notFound } from 'next/navigation';\n\nexport default function ComparisonPage({ params }: { params: { slug: string } }) {\n  const { slug } = params;\n  const [slug1, vs, slug2] = slug.split('-');\n\n  if (vs !== 'vs' || !slug1 || !slug2) {\n    notFound();\n  }\n\n  const product1 = (products as any[]).find((p: any) => p.slug === slug1);\n  const product2 = (products as any[]).find((p: any) => p.slug === slug2);\n\n  if (!product1 || !product2) {\n    notFound();\n  }\n\n  return (\n    <div className=\"max-w-4xl mx-auto py-12 px-4\">\n      <h1 className=\"text-4xl font-bold mb-8 text-center\">\n        {product1.name} vs {product2.name}\n      </h1>\n      <div className=\"grid md:grid-cols-2 gap-8\">\n        <div className=\"border rounded-lg p-6\">\n          <h2 className=\"text-2xl font-semibold mb-4\">{product1.name}</h2>\n          <p className=\"text-gray-600 mb-4\">{product1.description}</p>\n          <Link \n            href={product1.url} \n            target=\"_blank\" \n            rel=\"noopener noreferrer\"\n            className=\"text-blue-600 hover:underline\"\n          >\n            Visit Website\n          </Link>\n        </div>\n        <div className=\"border rounded-lg p-6\">\n          <h2 className=\"text-2xl font-semibold mb-4\">{product2.name}</h2>\n          <p className=\"text-gray-600 mb-4\">{product2.description}</p>\n          <Link \n            href={product2.url} \n            target=\"_blank\" \n            rel=\"noopener noreferrer\"\n            className=\"text-blue-600 hover:underline\"\n          >\n            Visit Website\n          </Link>\n        </div>\n      </div>\n    </div>\n  );\n}\n
